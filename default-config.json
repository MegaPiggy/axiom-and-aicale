{
  "$schema": "https://raw.githubusercontent.com/ow-mods/owml/master/schemas/config_schema.json",
  "enabled": true,
  "settings": {
    "alphaSFX": {
      "title": "Alpha SFX",
      "type": "toggle",
      "value": false,
      "tooltip": "My mod can replace some of the audio with alpha versions. Requires the loop to reset to take effect."
    }
  }
}
